name = "telegram-bot"
main = "src/index.js"
compatibility_date = "2024-09-23"

# Same D1 database as the backend worker (bind by database_id).
[[d1_databases]]
binding = "DB"
database_name = "company-ai-assistant-db"
database_id = "ddc3c8a6-665b-425c-9230-fa90799a1ffc"

# Set the bot token as a secret (not in this file):
#   npx wrangler secret put TELEGRAM_BOT_TOKEN
